<form method="post" action="{{ path('app_devices_delete', {'id': device.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ device.id) }}">
    <button class="btn">Delete</button>
</form>
