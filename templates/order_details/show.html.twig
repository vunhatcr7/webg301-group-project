{% extends 'base.html.twig' %}

{% block title %}OrderDetails{% endblock %}

{% block body %}
    <h1>OrderDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_detail.id }}</td>
            </tr>
            <tr>
                <th>OrderId</th>
                <td>{{ order_detail.OrderId }}</td>
            </tr>
            <tr>
                <th>DeviceId</th>
                <td>{{ order_detail.DeviceId }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_detail.Quantity }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ order_detail.Price }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ order_detail.Amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_details_index') }}">back to list</a>

    <a href="{{ path('app_order_details_edit', {'id': order_detail.id}) }}">edit</a>

    {{ include('order_details/_delete_form.html.twig') }}
{% endblock %}
